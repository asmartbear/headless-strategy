
// Resetting locations of things at the start or when the screen resizes
// function reflow() {

//     // Sidebars get aligned with the elements that reference them
//     $(".sidebar").offset(function(_,off){
//         const myId = $(this).attr('id');
//         off.top = $(`#${myId}-ref`).offset().top;
//         return off
//     });
// }
// $(window).resize(reflow);
